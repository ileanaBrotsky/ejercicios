
   <div class="d-flex justify-content-between align-items-center p-4">
     
         <h1>Este es mi carrito- id: </h1>
         <a type="button" class="btn btn-secondary " href="/delete_cart"><span class="h4">-</span> Quitar todos</a>
   </div>

     {{!-- <div class="col-12 d-flex flex-wrap justify-content-center p-4">
      {{#each productsInCart}}
            <div class="card m-2" style="width: 18rem;">
            <img src={{this.thumbnail}}class="card-img-top" style="height:250px" alt="...">
            <div class="card-body">
               <h5 class="card-title">{{this.title}}</h5>
               <p class="card-text">{{this.description}}</p>
               <div class="text-success p-2">${{this.price}}</div>
               <div class=" text-end">
                  <a href="/carts/64cecc22a83f9e03b7f393e4/{{this._id}}" class="btn btn-danger"><span class="h4">-</span> Quitar </a>
               </div>
              
            </div>
            </div>
       
      {{/each}}
      </div>
       <div class="d-flex justify-content-between align-items-center p-4">
      <h3>total: {{totalAmount}}</h3> 
    </div>--}}